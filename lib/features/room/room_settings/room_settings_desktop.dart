import 'package:flutter/material.dart';

class RoomSettingsDesktop extends StatelessWidget {
  const RoomSettingsDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
