import 'package:flutter/material.dart';

class PaymentDetailDesktop extends StatelessWidget {
  const PaymentDetailDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
