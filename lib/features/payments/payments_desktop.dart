import 'package:flutter/material.dart';

class PaymentsDesktop extends StatelessWidget {
  const PaymentsDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
